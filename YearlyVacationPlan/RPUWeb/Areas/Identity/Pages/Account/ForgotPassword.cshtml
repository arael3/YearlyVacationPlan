@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Nie pamiętasz hasła?";
}

<h1>@ViewData["Title"]</h1>
<h2>Wprowadź swój adres email.</h2>
<p>Jeżeli konto o wskazanym adresie email istnieje, zostanie na niego wysłana wiadomość z linkiem umożliwiającym reset hasła.</p>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="Input.Token" type="hidden" id="captcha-token" />
            <div class="form-floating">
                <input asp-for="Input.Email" class="form-control" aria-required="true" />
                <label asp-for="Input.Email" class="form-label"></label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">Wyślij prośbę o reset hasła</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <partial name="_GoogleReCaptchaScriptsPartial" />
}
